{
  "name": "medlens-monorepo",
  "version": "2.5.0",
  "private": true,
  "description": "MedLens — AI-Powered Clinical Information Intelligence Monorepo",
  "workspaces": [
    "packages/*",
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend && node -e \"const fs = require('fs'); fs.cpSync('frontend/dist', 'dist', {recursive: true});\"",
    "build:backend": "npm run build --prefix backend",
    "test": "npm run test --workspace=@medlens/clinical-engine",
    "start": "npm run start --prefix backend"
  },
  "keywords": ["clinical-ai", "gemini", "healthcare", "fhir", "sbar", "medical-intelligence"],
  "author": "K Ritish Reddy",
  "license": "MIT"
}
